# RazorShield AI Environment Configuration
# Optional: Provide Gemini API key for dynamic LLM reasoning (Fallback heuristic is automatic if omitted)
GEMINI_API_KEY=

# Razorpay Webhook Secret for HMAC verification
RAZORPAY_WEBHOOK_SECRET=razorshield_buildathon_secret

# Server Port
PORT=8000
